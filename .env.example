# Environment-specific configuration files:
# - `.env.development` — used for local dev (npm run dev)
# - `.env.production` — used for production builds (npm run build)
#
# Do NOT commit either file — both are in .gitignore for security.

# Example .env.development (local dev server):
# VITE_API_BASE=http://localhost:5000

# Example .env.production (production backend):
# VITE_API_BASE=https://medical-aiassist.onrender.com
